[workspace]
members = ["device", "cameleon", "gentl", "genapi", "impl", "ffi"]

[workspace.metadata.release]
# Use single tag for all crates (config for `cargo release`):
tag-name = "cameleon-v{{version}}"
